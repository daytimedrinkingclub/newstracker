{
    "name": "negative_summary",
    "description": "This tool will call an ai agent to analyse the data provided and return a pessimistic view point of the topic, try to share as much data that you found on search and your views to get a devil advocates view",
    "input_schema": {
      "type": "object",
      "properties": {
        "data_to_analyse": { 
            "type": "string", 
            "description": "Pass as much data / content in this string for the negative summary tool to analyse and share there views"
        }
      },
      "required": ["data_to_analyse"]
    }
}